topology = torus_credit;
k = 2;
n = 2;
routing_function = dim_order;
traffic = uniform;
injection_rate = 0.1;

// === Flow Control ===
num_vcs = 1;
vc_buf_size = 100;

// === Packet and Flit ===
packet_size = 1;

// === Simulation Control ===
sim_count = 500;

// === Results Output ===
print_activity = 0;
